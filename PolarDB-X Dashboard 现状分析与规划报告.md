# PolarDB-X Dashboard 现状分析与规划报告

## 1. 整体概览

### 1.1. 技术栈
- **前端**: Angular 19
- **后端**: Go Gin API 服务
- **UI 组件库**: Angular Material

### 1.2. 核心架构
- **前后端分离**: 前端通过 RESTful API 与后端通信。
- **模块化设计**: 采用标准的 Kubernetes Operator 模式，管理 `PolarDB-X` CRD 资源。
- **双核心模块**:
    1.  `polardbx-operator` (主模块): Operator 核心逻辑，运行于 K8s 集群内。
    2.  `backend` (UI 后端): 面向用户的 Web API 服务，独立部署，通过 `replace` 引用主模块 API 类型。

### 1.3. 主要功能模块
- **集群管理**: 集群的创建、查看、更新、删除及拓扑可视化。
- **备份恢复**: 手动/自动备份、Binlog 备份、时间点恢复（PITR）。
- **运维管理**: 监控、日志、参数模板、系统任务等全栈运维能力。
- **存储管理**: XStore 节点与副本的生命周期管理。

---

## 2. 功能模块现状详述

### 2.1. 集群管理

#### 已完成功能
- **集群生命周期管理**: 支持通过表单创建、删除集群，并提供跨命名空间的集群列表。
- **集群详情与拓扑**: 可查看集群详情、状态，并以拓扑图形式可视化 CN/DN/GMS/CDC 等核心组件。
- **节点（Pod）详情**: 提供 Pod 的日志查看、资源规格（Inspect）、YAML 定义以及 WebShell 终端。

#### 实现现状与待改进
- **WebShell 终端**:
    - **现状**: 已重构完成。前端采用标准 `xterm.js`，后端通过 WebSocket 将 K8s Exec Stream 代理到前端，交互延迟和稳定性显著改善。
    - **待改进**:
        - UX 优化：主题联动、字体调节、复制/粘贴提示。
        - 会话保持：支持容器切换热重连、心跳维持、历史会话恢复。
- **列表轮询**:
    - **现状**: 集群列表页存在每 5 秒轮询一次的机制。
    - **待改进**:
        - 路由切换后应取消订阅，避免后台持续请求。
        - 仅在页面可见时轮询，或提供轮询频率配置。

### 2.2. 备份恢复管理

#### 已完成功能
- **手动备份**: 支持创建即时全量备份，可指定存储位置 (S3/OSS/SFTP)。
- **定时备份**: 支持基于 Cron 表达式的自动化备份调度。
- **Binlog 备份**: 支持 Binlog 的实时备份与管理。
- **备份列表**: 可查看备份历史、状态，并支持删除操作。

#### 实现现状与待改进
- **备份创建**:
    - **现状**: 后端直接透传 `PolarDBXBackup` CRD 创建请求。若备份对象已存在，K8s `AlreadyExists` 错误会映射为 `409 Conflict`。
    - **待改进**: 需前端实现**幂等性**策略，如自动追加时间戳或随机后缀，避免命名冲突。
- **备份状态显示**:
    - **现状**: 列表状态通过映射 `status.phase` 字段实现。当对象刚创建或 `phase` 值未在前端预定义时，显示“未知”。
    - **待改进**:
        - 对象刚创建无状态时，应显示“创建中”等友好提示。
        - 需与后端对齐 `status.phase` 的完整枚举值，确保状态显示准确。
- **恢复功能**:
    - **现状**: 恢复管理模块已进行初步设计，规划了向导式恢复、任务管理和 PITR 功能。
    - **待改进**: 该模块尚未完全实现，需前后端协作完成 API 定义、流程开发与测试。

#### 未完成/待对齐事项
- **Provider 验证**: 尚未提供 OSS/SFTP 的 Secret 模板与 Sink 配置示例，未完成端到端验证。
- **进度可观测性**: 备份/恢复任务缺少进度反馈机制，建议通过 WebSocket/SSE 实现进度推送，并在前端提供进度条。

### 2.3. 运维管理

#### 已完成功能
- **监控管理 (PolarDBXMonitor)**: 支持监控任务的增删改查，可配置采集间隔。
- **参数模板 (ParameterTemplate)**: 支持参数模板的增删改查，并按组件分类。
- **系统任务 (SystemTask)**: 提供任务列表与详情查看。
- **日志收集 (LogCollector)**: 支持日志收集器的基本管理。
- **集群调优 (ClusterKnobs)**: 支持调优参数的分组展示与编辑。

#### 实现现状与待改进
- **设计与实现分离**: 各运维模块均已定义 CRD 并提供基础的 CRUD API，但**深度运维能力不足**，多为“CRD 配置器”。
- **监控**:
    - **现状**: 仅支持配置采集任务，无内置 Dashboard 和告警能力。
    - **待改进**: 需内置常用的 Grafana Dashboard 模板，并与 Prometheus/Alertmanager 联动，实现告警规则配置与通知。
- **参数管理**:
    - **现状**: 支持模板编辑，但缺少风险校验。
    - **待改进**: 需增加参数“预检”接口，验证风险与重启要求；完善批量应用与回滚策略。
- **任务与日志**:
    - **现状**: 仅能查看任务状态，日志为全量拉取。
    - **待改进**: 系统任务需支持流式日志查看；日志收集需提供 Pipeline 可视化配置。

#### 未完成/风险项
- **功能闭环**:
    - 监控：告警规则、通知渠道、多集群聚合视图。
    - 参数：批量应用/回滚、变更审计。
    - 任务：任务类型扩展（巡检、修复）、日志流式拉取。
- **自动化与智能化**:
    - 调优：A/B 测试、效果评估、危险操作风控。
    - 运维：常见错误自愈、趋势预测。

---

## 3. 近期核心变更总结
- **终端(WebShell)重构完成**:
    - 前端接入标准 `xterm.js` 流程，后端优化 Exec 通道，交互延迟大幅改善，解决了此前输入、回显、乱码等核心问题。
- **列表页请求频繁问题定位**:
    - 已定位到 `cluster-list` 页面存在持续轮询问题，并规划了优化方案。
- **备份创建 `409 Conflict` 问题定位**:
    - 已确认该问题由备份命名重复导致，需前端实现命名策略。

---

## 4. 未来规划 

### 4.1. 短期计划 
1.  **备份创建幂等性**: 前端为备份任务自动生成唯一名称（追加时间戳），解决 `409` 冲突。
2.  **列表轮询优化**: 在 `ngOnDestroy()` 中取消订阅，避免页面切换后继续请求。
3.  **终端 UX 优化**: 实现亮/暗主题联动、字体大小快捷调节、优化复制粘贴提示。
4.  **备份状态对齐**: 与后端对齐 `status.phase` 枚举值，完善前端状态映射。

### 4.2. 中期计划 
1.  **备份/恢复进度可视化**: 引入 WebSocket/SSE，实现任务进度的实时推送与展示。
2.  **恢复管理模块实现**: 完成向导式恢复、PITR 等核心功能的开发与交付。
3.  **运维能力增强**:
    - **监控**: 内置常用 Dashboard，提供告警规则配置界面。
    - **参数**: 实现参数预检、批量应用与变更审计。
4.  **WebShell 功能完善**: 支持会话保持、心跳检测与容器内多终端切换。

### 4.3. 长期规划
- **自动化运维**: 探索参数 A/B 测试、恢复演练自动化、任务编排可视化。
- **可观测性整合**: 在界面中深度整合 Metrics、Logging、Tracing，提供统一的可观测性平台。
- **多集群管理**: 提供统一的多集群管理视图，支持跨集群的资源查看与操作。

---

## 5. 测试与质量
- **后端覆盖率**: `pkg/api` 包综合覆盖率（单元+集成）为 **64.4%**。
- **集成测试**: 包含 29 项端点级用例，覆盖 CRUD、错误映射等。
- **其他包**: `pkg/k8s` 包覆盖率为 27.1%，待补齐。
- **前端测试**: E2E 与单元测试独立统计，不计入此覆盖率。