# Helm Charts 结构与部署指南

`charts/` 目录包含了部署 `polardbx-operator` 及其生态系统所需的全部 Helm Charts。Helm 是 Kubernetes 的包管理器，通过这些 Charts，可以实现一键式、可配置的部署。

### 目录结构概览

-   **`charts/polardbx-operator/`**: 部署 **PolarDB-X Operator** 自身的核心 Chart。
-   **`charts/polardbx-monitor/`**: 部署 **监控组件** (Prometheus & Grafana) 的 Chart。
-   **`charts/polardbx-logcollector/`**: 部署 **日志收集组件** (Filebeat & Logstash) 的 Chart。

---

## 1. `polardbx-operator` Chart

这是最核心的 Chart，用于将 `polardbx-operator` 部署到 Kubernetes 集群中。

-   **`Chart.yaml`**: 定义了 Chart 的基本信息，如名称和版本。
-   **`values.yaml`**: 提供了所有可配置的参数。**这是自定义部署时最需要关注的文件。** 关键配置包括：
    -   `image`: Operator 使用的镜像地址、仓库和版本。
    -   `resources`: Operator Pod 的 CPU 和内存资源限制。
    -   `filestreamServiceEndpoint`: HPFS 服务的地址，对于备份功能至关重要。
-   **`crds/`**: 包含了所有 `polardbx.aliyun.com` API 组下的自定义资源定义 (CRD) YAML 文件。Helm 在安装 Chart 时会自动将这些 CRD 应用到集群中。
-   **`templates/`**: 包含了所有 Kubernetes 资源的模板文件，例如：
    -   `deployment.yaml`: Operator Controller Manager 的部署。
    -   `webhook/`：与准入 Webhook 相关的 `ValidatingWebhookConfiguration`、`Service` 等资源的模板。
    -   `rbac.yaml`: Operator 所需的 `ClusterRole` 和 `ClusterRoleBinding`。

#### 部署命令示例

```bash
# 部署到 polardbx-operator-system 命名空间
helm install polardbx-operator ./charts/polardbx-operator \
  --namespace polardbx-operator-system \
  --create-namespace
```

---

## 2. `polardbx-monitor` Chart

此 Chart 用于部署一套完整的监控解决方案，基于 [Prometheus Operator](https://github.com/prometheus-operator/prometheus-operator) 构建。

-   **`Chart.yaml`**: Chart 的元信息。
-   **`values.yaml`**: 监控组件的配置项。可以控制是否启用 Prometheus 和 Grafana，以及它们的资源配置、存储等。
-   **`crds/`**: Prometheus Operator 自身的 CRD，例如 `Prometheus`、`ServiceMonitor`、`PodMonitor` 等。
-   **`templates/`**:
    -   Prometheus Operator 自身的部署。
    -   一个预配置的 `Prometheus` 实例，它通过 `ServiceMonitor` 自动发现并抓取 `polardbx-operator` 的 metrics。
    -   一个预配置的 `Grafana` 实例。
-   **`dashboard/`**: **一个关键特色**。此目录包含了大量预置的 Grafana 仪表盘 JSON 文件。这些仪表盘会在 Grafana 启动时通过一个 `ConfigMap` 自动导入，提供了对 PolarDB-X 集群性能指标的开箱即用的可视化。

#### 部署命令示例

```bash
helm install polardbx-monitor ./charts/polardbx-monitor \
  --namespace polardbx-monitor-system \
  --create-namespace
```

---

## 3. `polardbx-logcollector` Chart

此 Chart 用于部署日志收集代理，将 PolarDB-X 集群产生的各类日志（如慢查询日志、审计日志）收集并发送到统一的后端（如 Logstash）。

-   **`Chart.yaml`**: Chart 的元信息。
-   **`values.yaml`**: 主要配置项包括：
    -   `image`: Filebeat 和 Logstash 的镜像。
    -   `logstash.output.elasticsearch`: 如果使用 Elasticsearch 作为日志存储，这里需要配置其地址。
-   **`templates/`**:
    -   `log-collector-daemonset.yaml`: 以 `DaemonSet` 方式在每个节点上部署 Filebeat，用于从宿主机上挂载并收集日志文件。
    -   `logstash-deployment.yaml`: 部署 Logstash，用于接收 Filebeat 发来的日志，进行解析、过滤，并转发到最终的存储。

#### 部署命令示例

```bash
helm install polardbx-logcollector ./charts/polardbx-logcollector \
  --namespace polardbx-log-system \
  --create-namespace
```
description:
globs:
alwaysApply: false
---
