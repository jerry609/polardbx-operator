# Backend 服务架构

`backend` 目录包含一个独立的 Go Web 服务，为可视化平台提供 REST API。

## 核心组件

*   **`backend/main.go`**:
    *   Web 服务的主入口。
    *   使用 Gin 框架 (`github.com/gin-gonic/gin`)。
    *   定义了所有 API 路由。
    *   实现了 `KubeconfigAuthMiddleware` 中间件。

*   **`backend/pkg/api/handlers.go`**:
    *   实现了所有 API 端点的具体逻辑。
    *   通过 `clientFromContext` 辅助函数从 Gin 的上下文中获取 Kubernetes 客户端。

*   **`backend/pkg/k8s/client.go`**:
    *   封装了创建强类型 Kubernetes 客户端 (`client.Client`) 的逻辑。
    *   `NewClientFromKubeconfig` 函数是核心，它接收 `kubeconfig` 文件内容，并返回一个可以操作包括 `PolarDBXCluster` 在内的所有 CR 的客户端。

## 认证流程

1.  **初次连接 (`POST /api/v1/connect`)**:
    *   前端将 `kubeconfig` 内容放在**请求体**中发送。
    *   `api.Connect` handler 负责验证此 `kubeconfig` 的有效性。
    *   验证成功后，前端将 `kubeconfig` 进行 Base64 编码并保存在本地。

2.  **后续所有请求**:
    *   前端将 Base64 编码后的 `kubeconfig` 放在 **`X-Kubeconfig-B64` 请求头**中。
    *   `KubeconfigAuthMiddleware` 中间件会拦截这些请求，解码请求头，并使用 `k8s.NewClientFromKubeconfig` 创建一个临时的 K8s 客户端。
    *   中间件将创建好的客户端实例通过 `c.Set("k8sClient", client)` 注入到 Gin 的上下文中。
    *   后续的 handler (如 `api.ListClusters`) 通过上下文获取这个客户端实例来执行操作。
description:
globs:
alwaysApply: false
---
